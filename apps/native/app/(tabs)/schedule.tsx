import { View, Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

// Placeholder — Manus to build full schedule calendar view
export default function ScheduleScreen() {
  return (
    <SafeAreaView className="flex-1 bg-[#0f1117] items-center justify-center">
      <Text className="text-white text-xl font-bold">Schedule</Text>
      <Text className="text-gray-400 mt-2">Coming soon</Text>
    </SafeAreaView>
  )
}
